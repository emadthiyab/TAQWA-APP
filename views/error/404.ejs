<!DOCTYPE html>
<html lang="<%= lang %>" dir="<%= dir %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lang === 'ar' ? 'خطأ' : 'Error' %> - <%= lang === 'ar' ? 'نظام متابعة الأداء' : 'Performance Monitoring System' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #000000;
            --secondary: #d4af37;
            --gold: #d4af37;
            --light: #ffffff;
            --dark: #000000;
        }
        
        body {
            background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Cairo', sans-serif;
        }
        
        .error-card {
            background: var(--light);
            border-radius: 15px;
            padding: 3rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            border: 2px solid var(--gold);
            text-align: center;
            max-width: 500px;
            width: 100%;
        }
        
        .error-icon {
            font-size: 4rem;
            color: var(--gold);
            margin-bottom: 1.5rem;
        }
        
        .btn-luxury {
            background: linear-gradient(135deg, #d4af37 0%, #f4d03f 100%);
            border: none;
            border-radius: 8px;
            padding: 12px 30px;
            font-weight: 600;
            color: var(--dark);
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
        }
        
        .btn-luxury:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(212, 175, 55, 0.4);
            color: var(--dark);
        }
    </style>
</head>
<body>
    <div class="error-card">
        <div class="error-icon">
            <i class="fas fa-exclamation-triangle"></i>
        </div>
        <h1 class="display-4 fw-bold text-dark mb-3">
            <%= typeof status !== 'undefined' ? status : '500' %>
        </h1>
        <h3 class="text-gold mb-4">
            <%= lang === 'ar' ? 'حدث خطأ' : 'An Error Occurred' %>
        </h3>
        <p class="text-muted mb-4">
            <%= error || (lang === 'ar' ? 'عذراً، حدث خطأ في الخادم.' : 'Sorry, an error occurred on the server.') %>
        </p>
        <div class="d-grid gap-2 d-md-flex justify-content-md-center">
            <a href="/welcome?lang=<%= lang %>" class="btn btn-luxury me-md-2">
                <i class="fas fa-home me-2"></i>
                <%= lang === 'ar' ? 'الصفحة الرئيسية' : 'Home Page' %>
            </a>
            <a href="javascript:history.back()" class="btn btn-outline-dark">
                <i class="fas fa-arrow-<%= lang === 'ar' ? 'right' : 'left' %> me-2"></i>
                <%= lang === 'ar' ? 'رجوع' : 'Go Back' %>
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>